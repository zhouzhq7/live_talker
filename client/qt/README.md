# Live Talker Qt Client

基于PyQt6的Live Talker图形界面客户端。

## 功能特性

- 🎨 现代化的渐变UI设计
- 🎤 实时语音交互
- 📊 状态可视化（监听、思考、说话）
- 🎯 支持打断功能
- 📱 响应式布局

## 安装依赖

有两种方式安装Qt客户端依赖：

**方式1：从主requirements.txt安装（推荐）**
```bash
# 在主项目目录
pip install PyQt6>=6.6.0
```

**方式2：从客户端requirements.txt安装**
```bash
cd client/qt
pip install -r requirements.txt
```

**注意**：Qt依赖是可选的，如果只使用命令行界面，可以不安装。

## 运行

```bash
cd live_talker/client/qt
python main.py
```

## 项目结构

```
client/qt/
├── main.py              # 主入口
├── main_window.py       # 主窗口
├── talker_thread.py    # 后台线程（集成LiveTalker）
├── widgets/            # UI组件
│   ├── voice_sphere.py # 渐变球体
│   ├── status_bar.py   # 状态栏
│   └── control_buttons.py # 控制按钮
├── ui/                 # UI资源
│   └── styles.qss      # 样式表
└── requirements.txt    # 依赖项
```

## 使用说明

1. 点击麦克风按钮开始对话
2. 说话时球体会显示"正在听..."状态
3. AI处理时显示"正在思考..."
4. AI回复时显示"正在说话..."
5. 可以随时点击取消按钮停止

## 注意事项

- 确保已配置好环境变量（DEEPSEEK_API_KEY等）
- 需要安装FFmpeg（用于TTS）
- UI更新在主线程，LiveTalker处理在后台线程

